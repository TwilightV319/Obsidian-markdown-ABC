**标题相关**：可调字号、颜色、粗细、上下间距、是否显示下划线或分割线、标题前图标或装饰开关。

**正文排版**：正文大小、行距、段距、字体族、粗细、强调色、链接颜色、链接下划线开关。

**引用块**：边框颜色、边框粗细、背景色、内边距、引用符号样式、引用字体大小。

**列表**：项目符号样式、缩进量、行距、编号样式、列表层级间距。

**表格**：表格边框颜色、表头背景、条纹效果、单元格内边距、表格文字大小。

**代码相关**：行内代码颜色、背景、圆角；代码块字体大小、背景色、行号开关、语法高亮配色。

**侧边栏与文件树**：文件夹图标样式、文件夹缩进、文件名文字大小、活动项高亮色、悬停色、折叠箭头样式、侧边栏背景色、侧边栏纹理图片与透明度。

**面板与 UI**：滚动条样式、面板边框、卡片圆角、阴影强度、按钮样式、搜索框样式。

**嵌入与媒体**：嵌入块背景、边框、圆角；图片阴影、圆角、是否显示标题。

**全局色彩**：主色、强调色、背景层级色、边界色、超链接色、警告/提示信息颜色。

**布局与间距**：面板内边距、文档最大宽度、阅读模式列宽、移动端字号和布局参数。

**开关类功能**：启用或禁用特定主题特性、动画效果、极简模式、增强阅读模式、紧凑模式、边栏分隔线等。

**选择类功能**：切换不同预设视觉主题、不同标题风格套装、不同文件树风格、不同滚动条风格。

变量控制类：文本变量、数值变量、颜色变量、滑块变量，用于控制 CSS 片段或主题暴露出的参数。

导入导出：导出全部样式定制、导入别人共享的 Style Settings 配置。

如果你需要，我可以再给一份更极限、细到“可能出现在任意主题中的所有可控项总目录”。