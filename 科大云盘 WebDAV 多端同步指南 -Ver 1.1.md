# 前言
在科大的云盘由睿客网换到科大云盘后，虽然还有很多功能未实现（比如批量下载什么时候能出来），但是有一个巨大的优势就是增加了 WebDAV 功能。利用 WebDAV ，我们可以通过支持 WebDAV 的客户端（如操作系统资源管理器、第三方软件）像操作本地文件夹一样，对服务器上的文件进行**创建、删除、移动、复制、重命名**等操作。如果需要更简介明了的话，就可以理解为一个可以接入许多应用的网盘，你可以直接在设备或者客户端上访问网盘里的文件，并对其进行修改。在这里，就是通过多个设备上的 Obsidian 都安装同一个插件并进行相同配置，来进行多个设备上的文件同步。[^1]

---
# 同步配置
利用 WebDAV 同步需要依靠一些特定的第三方插件，我个人使用的是使用 Remotely Save 来进行同步，以下是具体做法： 
1. 首先在第三方插件商店里找到 Remotely Save 并下载、启用；
2. 然后来到[科大云盘](https://pan.ustc.edu.cn)的界面，登录后看到右上角的个人信息这里有一个 WebDAV 选项，点击进入；![[1(1).png|500]]
3. 随后出现的界面应该是与下图类似的，初次进入时可能会先展示一个初始密码，建议把这个密码保存一下，因为你确定密码就无法从这里看见它了（ 而且需要注意的是重置密码之后你先前用 WebDAV 绑定的所有东西都需要更改密码）![[2.png|500]]
4. 再回到 Remotely Save 这里，我们点击启用之后就可以在设置的第三方插件处找到它。首先远程服务需要选择 WebDAV ，然后在下面依次填写服务器地址、用户名和密码（和上面一张图顺序一致），剩下的可以不动。Depth header 选择 infinity 是可以正常使用的（~~虽然我目前也没研究这个的具体作用~~）![[Pasted image 20251213023806.png|550]]
5. 填写完之后还并不能直接连接，下拉后能看到这两行设置，第一行是同步到 WebDAV 的文件夹名，默认是你的仓库名，当然你也可以自行修改。**注意，Obsidian 本身并没有在科大云盘里面建一个新的资料库的权限，需要手动在科大云盘建立一个和你填的这个名字一致的资料库。** 在创建完之后再点下面的检查可否连接，如果成功的话就会跳出来“很好！可以连接上 WebDAV 服务器”的字样（在右上角），那么恭喜你到这一步已经成功完成了 WebDAV 的配置。![[Pasted image 20251213024038.png]]
6. 其他的设置就按照个人的喜好来配置即可，包括同步的频率、跳过大文件等。另外有一个设置可能需要注意一下，即下图的“修改超过百分比则终止同步”（在进阶设置中）。不少人会起初能正常同步，但是在一次更新后无法同步，很可能是这个设置的原因。它的默认值是50，即修改超过原文件的一半就会停止同步，可以根据个人需求提高一些来保证可以同步，但是如果设置为100也需要考虑到误操作改动过大而丢失信息的情况。这一行设置的上方也有冲突的解决策略，可以自行更改。![[Pasted image 20251213024650.png]]
7. 另外，也可以通过下面这个设置将不同设备上的插件、主题等配置进行同步。勾选这个选项并同步完成后，需要重启Obsidian或者在设置->第三方插件进行重新加载。但是这个功能疑似还不完善，可能有一定风险（虽然个人感觉没什么大问题），大家可以自行选择是否开启![[file-20251216142239128.png]]

以上就是利用 WebDAV ，通过配置 Remotely Save 插件来在多个设备上同步仓库文档的一个方式。同步时最后需要注意的是不同设备上的仓库需要保持一致，即仓库的名称一致或者 Remotely Save 设置中的基文件夹名称保持一致。希望可以对大家有帮助。

[^1]: 实际上可能不同插件也行，只要都用 WebDAV 应该都能连上，但是感觉没有什么必要那么自找麻烦）
