$$
\boldsymbol{\because\ \therefore\ \sim\ \otimes}
$$

# Markdown 是什么？
Markdown 是在 2004 年由 John Gruber 等人创造的一种**轻量型标记语言**。简单来说，它允许你用**纯文本符号**（比如 `#` 表示标题，`**` 表示加粗，`-` 表示列表项）来给文字添加格式。这些符号在编辑时清晰可见，而在预览或导出时，它们会被自动转换成排版漂亮的文档。它最大的特点就是**非常简洁、易读、易写**。

用大家最熟悉的场景说：**你在和 AI 对话的时候，AI 输出的文字其实全部都是 Markdown**（此处应有展示）。当你复制它们输出的内容时，可能会发现有 `#` 、 `$$` 等符号，这些就是 Markdown 的格式。此外，许多博客论坛、笔记应用（如 Obsidian）、以及 GitHub 项目里常见的 `Readme.md` 文件都广泛使用 Markdown，可见其运用范围极广。`.md` 就是 Markdown 文件的后缀格式。
![[GPT的输出.png|600]]

为什么说它是“**轻量化**”语言呢？这是它的一个重要优势。因为 Markdown 文件本质上是纯文本，所以它**打开速度极快，并且兼容性极强**（几乎所有平台和大量网页都支持）。这意味着你可以随时随地、在任何设备上轻松地创建、编辑和查看 Markdown 文件，非常方便，而且不用担心格式混乱或打不开的问题。

# Markdown 能干什么？
写**纯文本符号表示的格式**，它就能自动帮你转成格式漂亮的文档，比如加粗、标题、公式、列表等等。你可以用它来：
1. 做学科笔记，写公式；
2. 写各种日常记录，如实验记录等；
3. 当一个轻量化的 Word，写各种文案、文章；
4. 做任务管理，当备忘录；
5. 社团管理、写小说、写旅行计划……

你可能会想，这玩意为什么能干这么多事？当你了解了 Markdown 和 Obsidian 的用法就能理解其便利之处了。

# 一些题外话：LaTeX 又是什么？
简单来说，LaTeX 是一种**专业的排版系统**。它不是用来写笔记的，而是用来**制作高质量的学术文档**，比如毕业论文、学术期刊文章、复杂的数学公式推导、书籍等等。你可以把它想象成一个功能极其强大、对排版细节有极致控制力的“编程语言”，专门用于生成精美的 PDF 文档。

某种意义上，你可以把 Markdown 看做**简化了语法复杂度**的 LaTeX ，因为两者（尤其是对于理工科）而言有许多类似的使用场景，包括写笔记和讲义等，在这些情况下使用 Markdown 会比 LaTeX 便捷非常多而且也能达到很好的效果，避免你在排版上浪费太多时间。当然，在一些**对格式要求十分严格**的情况下，如一些老师的大物实验报告和学术论文，LaTeX 还是比 Markdown 更为合适的。但是如果老师也没有明确的要求，使用 Markdown 是完全可行的

P.S. 实际上还有一个东西叫 typst ，据说是跟 md 的语法复杂度相当而且能实现 LaTeX 差不多的排版能力，但是我不会用，所以就不提了（）有兴趣的话大家可以后续了解一下

# 题外话之二：html 与 css？
如果已经接触了一部分 Markdown 相关内容，可能会看到两个 Markdown 以外的但是可以写在 .md 文件里面的语法：**HTML** 与 **CSS**. 这两个语法可以理解为是 md 背后的语法，你所看到的文字都是 Markdown 语法被转换为了 HTML 语法然后呈现出来的，CSS 则是用于控制细节的设置文件，如字体、颜色等。因此在写 Markdown 文件时如果有一定特殊需求也可以直接插入 HTML 语法或者编写一个 .css 文件来实现。但是这部分内容过于复杂，所以我们也不会提这部分内容，需要的话 AI 会给你的（雾）

# Obsidian 能干什么？
随便举几个例子：
1. **方便地导出图片和 PDF**；
2. 像浏览器里的“网页收藏夹”一样**管理你本地的笔记/文件**等等；
3. 方便地打 LaTeX 公式；
4. 方便地本地搜索；
5. 方便地记日记和一切类似的东西；
6. 等等等等……

总之，Markdown 的用法十分多样，并且是大家会极频繁接触的语言，欢迎大家学习。